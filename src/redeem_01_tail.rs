//! Заглушка redeem-01 tail.
//!
//! Сейчас стратегия намеренно не покупает. После исправления
//! `currency_price_vs_beat_pct` в дампах нужно заново собрать правило входа,
//! чтобы постараться повторить поведение бота `0x50f7`, без подгонки случайных
//! констант. План работы:
//!
//! 1. Пересобрать join сделок бота с локальными xframes по конкретным
//!    `market_id`/`asset_id`, чтобы для каждого fill иметь: сторону, цену
//!    исполнения, ask-liquidity до 10c, `currency_implied_prob`, секунду от
//!    старта/до закрытия и `currency_price_vs_beat_pct`.
//! 2. Разделить покупки бота по coin/period и типу состояния рынка: дешёвый
//!    хвост, дешёвый токен уже на выигрышной стороне, покупка 99% у закрытия и
//!    проигрышные докупки другой стороны.
//! 3. На каждом coin/period сравнить PnL и invested только по тем входам,
//!    которые локальная симуляция реально могла исполнить по стакану, а не по
//!    одной лишь цене из activity API.
//! 4. Вывести формулу размера входа из наблюдаемых параметров: времени до
//!    закрытия, entry price/best ask, доступной ликвидности, probability,
//!    отклонения spot от `price_to_beat` и режима последних рынков.
//! 5. Проверить формулу на дампах против ориентира бота: общий invested порядка
//!    его расходов за период и близкий tail/redeem PnL, отдельно по coin/period.
//! 6. Только после этого заменить эту заглушку минимальным правилом входа; если
//!    для coin/period данных нет, не додумывать fallback.

use crate::history_sim::StrictBook;
use crate::xframe::{SIZE, XFrame};

pub(crate) fn redeem_01_tail_entry_size(
    _frame: &XFrame<SIZE>,
    _strict_book: Option<&StrictBook>,
    _entry_prob: f64,
    _bankroll: f64,
) -> Option<f64> {
    None
}
